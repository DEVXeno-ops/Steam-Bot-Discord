````markdown
# SteamThailand Bot

> 🛡️ Discord Bot สำหรับจัดการระบบ Blacklist Steam Cheater และระบบ Donation ผ่าน Steam Trade Offer  
> พัฒนาโดย xeno (ริน)

---

## 🔥 ฟีเจอร์หลัก

- ✅ **ระบบ Blacklist Steam Cheater**  
  ตรวจสอบและบล็อกผู้ใช้ที่ใช้โปรแกรมช่วยเล่น พร้อมบันทึกเหตุผลและเวลา  
- 🎁 **ระบบ Donation / Steam Trade Offer**  
  แสดงรายชื่อผู้รับบริจาค ส่งลิงก์ trade offer ทาง DM และบันทึกประวัติ  
- 🔐 **ระบบตรวจสอบความปลอดภัย**  
  ตรวจสอบ SteamID64 และลิงก์ Trade Offer ว่าถูกต้อง  
- 📜 **ระบบบันทึก Log**  
  เก็บประวัติการแบนและบริจาค พร้อมส่งแจ้งเตือนใน Discord Log Channel  
- 💬 **Embed แจ้งเตือนสวยงาม**  
  ส่ง Embed แจ้งเตือนเมื่อมีการดำเนินการต่าง ๆ  
- 🎮 **รองรับ Slash Commands เต็มรูปแบบ**  
  ใช้งานง่ายและสะดวกผ่านคำสั่งใน Discord

---

## ⚙️ การติดตั้งและใช้งาน

1. ติดตั้ง [Node.js v16.9+](https://nodejs.org/en/download/)
2. โคลนหรือดาวน์โหลดโปรเจกต์นี้
3. รันคำสั่งติดตั้ง dependencies

   ```bash
   npm install
````

4. ตั้งค่าไฟล์ `config.js` (หรือแก้ไขในโค้ดหลัก)

   * ใส่ `discordToken`
   * ใส่ `logChannelId`
   * ใส่ `adminRoleId`

5. รันบอทด้วยคำสั่ง

   ```bash
   node index.js
   ```

6. บอทจะลงทะเบียนคำสั่ง Slash อัตโนมัติและพร้อมใช้งาน

---

## 📝 คำสั่งหลัก (Slash Commands)

| คำสั่ง                | คำอธิบาย                           | สิทธิ์ใช้งาน           |
| --------------------- | ---------------------------------- | ---------------------- |
| `/setup donate`       | เพิ่มผู้รับบริจาค (ต้องเป็นแอดมิน) | Admin เท่านั้น         |
| `/setup removedonate` | ลบผู้รับบริจาค (ต้องเป็นแอดมิน)    | Admin เท่านั้น         |
| `/blacklist`          | เพิ่ม SteamID ในบัญชีดำ            | ทุกคน (แต่มีการยืนยัน) |
| `/check`              | ตรวจสอบ SteamID ในบัญชีดำ          | ทุกคน                  |
| `/donate`             | ขอรับลิงก์บริจาค                   | ทุกคน                  |
| `/history`            | ดูประวัติบัญชีดำทั้งหมด            | ทุกคน                  |
| `/donationhistory`    | ดูประวัติการบริจาคทั้งหมด          | ทุกคน                  |

---

## 🛠️ โครงสร้างไฟล์สำคัญ

* `index.js` — ไฟล์หลักของบอท
* `commands.js` — ลงทะเบียนคำสั่ง Slash
* `data.js` — ฟังก์ชันโหลดและบันทึกข้อมูล JSON
* `embeds.js` — ฟังก์ชันสร้าง Embed สวยงาม
* `utils.js` — ฟังก์ชันช่วยเหลือ เช่น ตรวจสอบ SteamID, ลิงก์, sanitize input
* `data.json` — ไฟล์เก็บข้อมูล blacklist และ donationHistory

---

## 🔒 ความปลอดภัย

* จำกัดจำนวนการแบนต่อวัน (default 5 ครั้งต่อผู้ใช้ต่อวัน)
* ตรวจสอบสิทธิ์แอดมินก่อนตั้งค่าผู้รับบริจาค
* ป้องกันคำสั่งผิดพลาดด้วยการ sanitize input และตรวจสอบรูปแบบข้อมูล

---

## 📣 ข้อแนะนำ

* อย่าเผย Token บอทสู่สาธารณะ
* ควรสำรองข้อมูล `data.json` เป็นระยะ
* ปรับแต่งค่าต่าง ๆ ใน `config` ให้เหมาะสมกับเซิร์ฟเวอร์ของคุณ

---

## 📜 ใบอนุญาต (License)

MIT License

---

## 🙏 เครดิต

SteamThailand Bot โดย xeno (ริน)
ขอขอบคุณทุกคนที่สนับสนุนและใช้งานบอทนี้

---

> **หมายเหตุ:** หากพบปัญหาหรือมีข้อเสนอแนะสามารถเปิด Issue ใน GitHub ได้เลยครับ!

```

```
